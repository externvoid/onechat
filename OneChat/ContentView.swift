import OneChatLogic
import SwiftUI

struct ContentView: View {
  var body: some View {
    ChatStateView()
  }
}

#Preview{
//  ContentView(messages: messages)
  ContentView()
    .frame(width: 340, height: 640)
}
